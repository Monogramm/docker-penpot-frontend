dist: trusty
sudo: required

services: docker

language: bash

branches:
  only:
    - master
    - develop

before_script:
  - env | sort
  - dir="images/${VERSION}/${VARIANT}"

script:
  - cd "$dir"
  - docker-compose build
  - docker images
  - travis_retry docker-compose up -d && sleep 60
  - docker-compose ps
  - docker-compose logs
  #- docker-compose ps "uxbox_db" | grep "Up"
  #- docker-compose ps "uxbox_backend" | grep "Up"
  - docker-compose ps "uxbox_frontend" | grep "Up"

notifications:
  email: false

env: # Environments
    - VERSION=build-refactor VARIANT=alpine
    - VERSION=build-refactor VARIANT=mainline
